import { Button } from "antd";
import classNames from 'classnames/bind';
import styles from './Share.module.scss';

function Share() {
      return ( <Button block type="text">Share</Button> );
}

export default Share;